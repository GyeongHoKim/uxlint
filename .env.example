# MCP Server Configuration
# Server command to run (default: npx)
MCP_SERVER_COMMAND=npx

# Server arguments (default: @playwright/mcp@latest)
# MCP_SERVER_ARGS=@playwright/mcp@latest

# Browser type for Playwright automation
# Options: chrome, firefox, webkit, msedge
MCP_BROWSER=chrome

# Run browser in headless mode
# Set to 'false' to show browser window
MCP_HEADLESS=true

# Operation timeout in milliseconds
# Default: 30000 (30 seconds)
MCP_TIMEOUT=30000

# AI Service Configuration
# Anthropic API key for UX analysis (required)
UXLINT_ANTHROPIC_API_KEY=your_api_key_here

# AI model name (optional, defaults to claude-3-5-sonnet-20241022)
UXLINT_AI_MODEL=claude-3-5-sonnet-20241022
