# MCP Server Configuration
# Server command to run (default: npx)
MCP_SERVER_COMMAND=npx

# Server arguments (default: @playwright/mcp@latest)
# MCP_SERVER_ARGS=@playwright/mcp@latest

# Browser type for Playwright automation
# Options: chrome, firefox, webkit, msedge
MCP_BROWSER=chrome

# Run browser in headless mode
# Set to 'false' to show browser window
MCP_HEADLESS=true

# Operation timeout in milliseconds
# Default: 30000 (30 seconds)
MCP_TIMEOUT=30000

# AI Service Configuration
# AI provider to use (optional, defaults to 'anthropic')
# Options: anthropic, openai, ollama, xai
UXLINT_AI_PROVIDER=anthropic

# Anthropic Configuration (required if UXLINT_AI_PROVIDER=anthropic or not set)
# Get your API key from https://console.anthropic.com/
UXLINT_ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI Configuration (required if UXLINT_AI_PROVIDER=openai)
# Get your API key from https://platform.openai.com/api-keys
# UXLINT_OPENAI_API_KEY=your_openai_api_key_here

# Ollama Configuration (required if UXLINT_AI_PROVIDER=ollama)
# Base URL for your Ollama server (defaults to http://localhost:11434)
# UXLINT_OLLAMA_BASE_URL=http://localhost:11434

# xAI (Grok) Configuration (required if UXLINT_AI_PROVIDER=xai)
# Get your API key from https://x.ai/
# UXLINT_XAI_API_KEY=your_xai_api_key_here

# AI model name (optional)
# Defaults vary by provider:
# - Anthropic: claude-sonnet-4-5-20250929
# - OpenAI: gpt-4o
# - Ollama: llama3.1
# - xAI: grok-4
UXLINT_AI_MODEL=claude-sonnet-4-5-20250929
